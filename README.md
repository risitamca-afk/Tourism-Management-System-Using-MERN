# Tourism-Website
run npm install inside project folder for backend and inside fronend for frontend case 
MONGO_URI= Your Mongo urls
PORT=your port
JWT_SECRET=Your secret key 
JWT_SECRET_ADMIN=Your secret key in case of admin 
NODE_ENV=development
# MAILTRAP_TOKEN= for sending mail token 
MAILTRAP_TOKEN= for sending mail token 
CLINT_URL=your url from frontend view
ADMIN_USERNAME=admin username
ADMIN_PASSWORD=admin password
